--- CPP/Clipper2Lib/src/clipper.engine.cpp	2026-09-25 23:43:13.867066830 +0000
+++ CPP/Clipper2Lib/src/clipper.engine.cpp (root-cause check only)	2026-09-26 01:06:33.482444241 +0000
@@ -440,7 +440,7 @@
 
   inline bool IsVerySmallTriangle(const OutPt& op)
   {
-    return op.next->next == op.prev &&
+    return false && op.next->next == op.prev &&
       (PtsReallyClose(op.prev->pt, op.next->pt) ||
         PtsReallyClose(op.pt, op.next->pt) ||
         PtsReallyClose(op.pt, op.prev->pt));
